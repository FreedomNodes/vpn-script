#!name = Cloudflare
#!desc = Cloudflare DNS
#!requirement=CORE_VERSION>=20 && (SYSTEM = 'iOS' || SYSTEM = 'macOS')
#!category=DNS Server
#!author=Kien Tran

[General]
dns-server = %APPEND% 1.1.1.1, 1.0.0.1
